% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_configuration.R
\name{extract_inputtypes}
\alias{extract_inputtypes}
\title{Extract a List of inputTypes from the Project Configuration}
\usage{
extract_inputtypes(nested_list, parent_name = NULL, category_name = NULL)
}
\arguments{
\item{nested_list}{A configuration list as returned by \code{\link[=get_configuration]{get_configuration()}},
or a sub-list thereof during recursion.}

\item{parent_name}{Character. Name of the supercategory currently being
processed. \code{NULL} on the initial call; set internally during recursion.}

\item{category_name}{Character. Name of the (sub)category currently being
processed. \code{NULL} on the initial call; set internally during recursion.}
}
\value{
A list of named lists, each with elements \code{category}, \code{parent},
\code{fieldname}, and \code{inputType}, one entry per field found. Returns \code{NULL}
if no fields are found.
}
\description{
Internal recursive helper to \code{\link[=parse_field_inputtypes]{parse_field_inputtypes()}}. Traverses the nested
\code{categories} / \code{trees} structure of an \code{idaifield_config} and collects one
entry per field, recording its category, parent supercategory, field name,
and inputType.
}
\details{
The config tree has two levels of nesting that matter here:
\itemize{
\item Top level (\code{categories}): supercategories (e.g. \code{Operation}, \code{Find}).
\item Second level (\code{trees}): subcategories (e.g. \code{Trench} inside \code{Operation}).
}

Fields live inside \code{item$groups[[group]]$fields}. The function visits both
levels and records \code{parent} as the supercategory in both cases.
}
\examples{
\dontrun{
conn <- connect_idaifield(serverip = "localhost",
                          pwd = "hallo",
                          project = "rtest")
config <- get_configuration(connection = conn)
input_types <- extract_inputtypes(config)
}
}
\seealso{
\code{\link[=parse_field_inputtypes]{parse_field_inputtypes()}}
}
\keyword{internal}
