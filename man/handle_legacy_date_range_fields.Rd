% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify_utilities.R
\name{handle_legacy_date_range_fields}
\alias{handle_legacy_date_range_fields}
\title{Update Legacy Two-Field Date Format in an iDAI.field Resource}
\usage{
handle_legacy_date_range_fields(resource)
}
\arguments{
\item{resource}{A single resource (one element from an
\code{idaifield_resources} list).}
}
\value{
The resource with \code{beginningDate} and \code{endDate} removed and
replaced by a \code{date}-list to be handled by \code{\link[=handle_date_input]{handle_date_input()}}.
Returned unchanged if neither field is present.
}
\description{
Some older iDAI.field resources store date ranges as two separate top-level
fields — \code{beginningDate} and \code{endDate} — rather than a single \code{date} list.
Resources in Field Desktop are only updated to the new format when they are
actively worked on and saved. This function detects those fields and
brings them into the expected, current format, removing the originals.
}
\details{
Resources without both \code{beginningDate} and \code{endDate} are returned unchanged.
If only one of the two fields is present, the other is set to \code{NA}.
}
\examples{
\dontrun{
resource <- list(
  identifier    = "legacyResource",
  category      = "Feature",
  beginningDate = "12.03.2026",
  endDate       = "13.03.2026"
)
handle_legacy_date_fields(resource)
}
}
\seealso{
\itemize{
\item \code{\link[=handle_date_input]{handle_date_input()}} for the current and legacy single-field formats.
\item \code{\link[=simplify_single_resource]{simplify_single_resource()}} which calls this as a pre-processing step.
}
}
