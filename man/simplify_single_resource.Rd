% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify_idaifield_resources.R
\name{simplify_single_resource}
\alias{simplify_single_resource}
\title{Simplify a Single Resource from an iDAI.field / Field Desktop Database}
\usage{
simplify_single_resource(
  resource,
  index = NULL,
  config = NULL,
  replace_uids = TRUE,
  find_layers = TRUE,
  keep_geometry = TRUE,
  silent = FALSE
)
}
\arguments{
\item{resource}{One element from an \code{idaifield_resources} list.}

\item{index}{A data.frame as returned by \code{\link[=get_uid_list]{get_uid_list()}} or
\code{\link[=get_field_index]{get_field_index()}}. Required for UUID replacement and layer detection.}

\item{config}{An \code{idaifield_config} object as returned by
\code{\link[=get_configuration]{get_configuration()}}. Required when \code{find_layers = TRUE}.}

\item{replace_uids}{logical. Should UUIDs in relations be replaced with
human-readable identifiers from \code{index}? Default is TRUE.}

\item{find_layers}{logical. Should the containing layer be detected and
added as \code{relation.liesWithinLayer}? Default is TRUE.}

\item{keep_geometry}{logical. Should geometry be kept as a GeoJSON string?
Default is TRUE.}

\item{silent}{logical. Should messages be suppressed? Default is FALSE.}
}
\value{
A single resource with relations flattened, geometry handled,
and simple fields unlisted to vectors.
}
\description{
Helper function to \code{\link[=simplify_idaifield]{simplify_idaifield()}}. Transforms a single resource
from an \code{idaifield_resources} list into a flatter, more R-friendly
structure.
}
\examples{
\dontrun{
index <- get_uid_list(docs)
config <- get_configuration(conn)
simpler <- simplify_single_resource(docs[[1]],
  index = index,
  config = config,
  keep_geometry = FALSE
)
}
}
\seealso{
\code{\link[=simplify_idaifield]{simplify_idaifield()}}, \code{\link[=fix_relations]{fix_relations()}}, \code{\link[=find_layer]{find_layer()}}
}
\keyword{internal}
