% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify_utilities.R
\name{handle_dropdownrange_input}
\alias{handle_dropdownrange_input}
\title{Handle a \code{dropdownRange} Input Field from an iDAI.field Resource}
\usage{
handle_dropdownrange_input(dropdownRangeInput, name)
}
\arguments{
\item{dropdownRangeInput}{The value of a single \code{dropdownRange}-type field
from a resource, i.e. \code{period} from the default Configuration. Expects a
list with at minimum a \code{value} element and optionally an \code{endValue}.}

\item{name}{Character. The name of the field being processed (e.g.
\code{"period"}). Used to name the output elements as \verb{<name>.start} and
\verb{<name>.end}.}
}
\value{
A named list with two elements:
\describe{
\item{\if{html}{\out{<name>}}.start}{The start value as a character string.}
\item{\if{html}{\out{<name>}}.end}{The end value as a character string, or the start
value if no endValue was found.}
}
Unexpected input formats return \code{NA} for both values.
}
\description{
Flattens a single \code{dropdownRange}-type field value from an iDAI.field
resource into a named list with two elements: \verb{<name>.start} and
\verb{<name>.end}. If only the start value exists, both \verb{<name>.start} and
\verb{<name>.end} are set to the start value.
}
\examples{
\dontrun{
handle_dropdownrange_input(list(value = "Classical"), "period")

# Current format, date range with time
handle_date_input(
  list(value = "Classical", endValue = "Hellenistic"),
  "period"
)
}
}
\seealso{
\itemize{
\item \code{\link[=simplify_idaifield]{simplify_idaifield()}} which dispatches to this function.
}
}
