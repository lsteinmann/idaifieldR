% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplify_utilities.R
\name{handle_date_input}
\alias{handle_date_input}
\title{Handle a \code{date} Input Field from an iDAI.field Resource}
\usage{
handle_date_input(dateInput, name)
}
\arguments{
\item{name}{Character. The name of the field being processed (e.g.
\code{"date"}, \code{"restorationDate"}). Used to name the output elements as
\verb{<name>.start} and \verb{<name>.end}.}

\item{value}{The value of a single \code{date}-type field from a resource, i.e.
\code{resource$date} or \code{resource$restorationDate}. Either a character string
(legacy single-field format) or a list with at minimum a \code{value} element
and an \code{isRange} logical (current format).}
}
\value{
A named list with two elements:
\describe{
\item{\if{html}{\out{<name>}}.start}{The start date as a character string.}
\item{\if{html}{\out{<name>}}.end}{The end date as a character string, or \code{NA} if the
field is not a range.}
}
}
\description{
Flattens a single \code{date}-type field value from an iDAI.field resource into
a named list with two elements: \verb{<name>.start} and \verb{<name>.end}. Handles
both the legacy format (a plain character string) and the current format
(a list with \code{value}, optional \code{endValue}, and \code{isRange}).
}
\details{
Date strings are returned exactly as stored in the database — no parsing,
type conversion, or format normalisation is applied. Possible formats
include \code{"DD.MM.YYYY"}, \code{"DD.MM.YYYY HH:MM"}, \code{"MM.YYYY"}, and \code{"YYYY"}.

For the legacy two-field format (\code{beginningDate} / \code{endDate} as separate
top-level keys in the resource), use \code{\link[=handle_legacy_date_range_fields]{handle_legacy_date_range_fields()}} on
the whole resource before per-field dispatch.
}
\examples{
\dontrun{
# Current format, single date
handle_date_input(list(value = "12.03.2026", isRange = FALSE), "date")

# Current format, date range with time
handle_date_input(
  list(value = "19.08.2017 17:25", endValue = "20.08.2017 11:09", isRange = TRUE),
  "date"
)

# Legacy plain string
handle_date_input("12.03.2026", "date")

# Named after a custom date field
handle_date_input(list(value = "2025", isRange = FALSE), "restorationDate")
}
}
\seealso{
\itemize{
\item \code{\link[=handle_legacy_date_range_fields]{handle_legacy_date_range_fields()}} for the legacy two-field format.
\item \code{\link[=simplify_single_resource]{simplify_single_resource()}} which dispatches to this function.
}
}
