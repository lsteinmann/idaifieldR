# Check if object is a "idf_connection_settings" and stop if not.

Check if object is a "idf_connection_settings" and stop if not.

## Usage

``` r
stop_if_not_idf_connection_settings(x)
```

## Arguments

- x:

  an object

## Value

nothing
